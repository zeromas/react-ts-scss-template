const useAuthentication = () => {

  const signIn = () => {
  }

  const signUp = () => {
  }

  const signOut = () => {
  }

  const verifyEmail = () => {
  }
  
  const requestResetPassword = () => {
  }

  const changePassword = () => {
  }

  return { signOut, signIn, signUp, verifyEmail, requestResetPassword, changePassword }
}

export default useAuthentication
